class Pose():
    """Represents a pose of the dibi game the robot can assume.
    """
    def assume(self, motion):
        """Assumes, i.e. makes the pose.

        Args:
            motion (ALMotion): instance of ALMotion to execute the pose.
        """
        pass